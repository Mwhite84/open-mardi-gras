---
description: Have oc-smith craft or refine an opencode instrument (agent, skill, or command)
agent: oc-smith
---

Craft or refine the following opencode instrument: $ARGUMENTS

Use the `authoring-opencode` skill. Start by classifying what you are building —
a who (agent), a how (skill), or an ask (command), and for a how-or-ask, whether
the agent should be able to invoke it (skill) or only the user (command). If the
request mixes instruments, separate the pieces and place each correctly.

Reach for `customize-opencode` for exact agent/skill frontmatter, file
locations, and permission semantics. When you finish, remind me to restart
opencode so the change takes effect.
